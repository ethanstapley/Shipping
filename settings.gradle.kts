rootProject.name = "Shipping"

pluginManagement {
    repositories {
        google()
        gradlePluginPortal()
        mavenCentral()
    }
    plugins {
        id("org.jetbrains.compose") version "1.5.0"
    }
}
